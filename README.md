# 🛒 Express.js Products API

A simple RESTful API built with **Express.js** that manages a list of products.  
This project demonstrates CRUD operations, middleware usage, authentication, validation, and error handling.

---

## 🚀 How to Run the Server

### 1️⃣ Install Dependencies
Make sure you have Node.js installed, then run:
```bash
npm install
